import { theme } from "../../colors.json";

export const backModalStyles = `flex-1 bg-transparent items-center justify-center`;

export const foreModalStyles = `h-[80%] w-[80%] bg-[${theme.sapphire}] border-solid border-2 border-[${theme.orange}] items-center justify-around`;
