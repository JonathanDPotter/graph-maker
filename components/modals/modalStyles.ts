import { theme } from "../../colors.json";

export const backModalStyles = `flex-1 bg-transparent items-center justify-center`;

export const foreModalStyles = `bg-[${theme.sapphire}] border-solid border-2 border-[${theme.orange}] justify-around p-2`;

export const dataLabelInput = `flex-row border-solid border-2 border-[${theme.black}] items-end py-2 px-2 justify-between h-12`;

export const dataLabel = `text-lg`;

export const dataInput = `text-lg flex-1 text-[${theme.champagne}] border-b border-[${theme.champagne}]`;
